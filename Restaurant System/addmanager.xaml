<Window x:Class="Restaurant_System.addmanager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add Manager"
        Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Window.Resources>
        <DropShadowEffect x:Key="CardShadow" ShadowDepth="4" Color="#888" BlurRadius="10"/>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Border Padding="20" CornerRadius="15" Background="White" Effect="{StaticResource CardShadow}">
            <StackPanel>
                <TextBlock Text="➕ Add New Manager"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="#333"
                           Margin="0,0,0,20"
                           HorizontalAlignment="Center"/>

                <Label Content="Username" FontWeight="SemiBold" FontSize="14" Foreground="#444"/>
                <TextBox x:Name="UsernameBox"
                         Height="30" Padding="5"
                         Margin="0,0,0,10"
                         BorderBrush="#2196F3" BorderThickness="1"/>

                <Label Content="Password" FontWeight="SemiBold" FontSize="14" Foreground="#444"/>
                <PasswordBox x:Name="PasswordBox"
                             Height="30" Padding="5"
                             Margin="0,0,0,20"
                             BorderBrush="#2196F3" BorderThickness="1"/>

                <Button Content="Add"
                        HorizontalAlignment="Center"
                        Style="{StaticResource PrimaryButton}"
                        Click="AddManager_Click"
                        Margin="0,0,0,10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>