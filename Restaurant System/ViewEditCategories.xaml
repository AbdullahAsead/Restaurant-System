<Window x:Class="Restaurant_System.ViewEditCategories"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="إدارة الأقسام"
        Height="520"
        Width="820"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">

    <Window.Resources>
        <!-- 🔹 ستايل الأزرار داخل الجدول -->
        <Style x:Key="GridActionButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="4,0,4,0"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ✅ عنوان الصفحة -->
        <TextBlock Text="📂 إدارة الأقسام"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#1976D2"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

        <!-- ✅ منطقة البحث -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">

            <!-- 🔹 مربع بحث الاسم -->
            <Grid Margin="5">
                <TextBox x:Name="NameSearchBox"
                         Width="180"
                         Height="30"
                         VerticalContentAlignment="Center"
                         TextChanged="SearchBox_TextChanged"/>
                <TextBlock Text="ابحث بالاسم"
                           Foreground="Gray"
                           VerticalAlignment="Center"
                           Margin="8,0,0,0"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=NameSearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- 🔹 مربع بحث الوصف -->
            <Grid Margin="5">
                <TextBox x:Name="DescSearchBox"
                         Width="180"
                         Height="30"
                         VerticalContentAlignment="Center"
                         TextChanged="SearchBox_TextChanged"/>
                <TextBlock Text="ابحث بالوصف"
                           Foreground="Gray"
                           VerticalAlignment="Center"
                           Margin="8,0,0,0"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=DescSearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <Button Content="🔍 بحث"
                    Width="90"
                    Height="30"
                    Margin="5"
                    Background="#1976D2"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="SearchButton_Click"/>

            <Button Content="🧹 مسح"
                    Width="90"
                    Height="30"
                    Margin="5"
                    Background="#BDBDBD"
                    Foreground="Black"
                    FontWeight="Bold"
                    Click="ClearSearchButton_Click"/>
        </StackPanel>

        <!-- ✅ جدول عرض الأقسام -->
        <DataGrid x:Name="CategoriesGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  RowHeight="35"
                  FontSize="14"
                  Margin="0"
                  AlternatingRowBackground="#E3F2FD"
                  GridLinesVisibility="All"
                  BorderBrush="#90CAF9"
                  BorderThickness="1"
                  HorizontalScrollBarVisibility="Disabled">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="الوصف" Binding="{Binding Description}" Width="*"/>

                <DataGridTemplateColumn Header="التحكم" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✏️ تعديل"
                                        Background="#FFF59D"
                                        Foreground="Black"
                                        Style="{StaticResource GridActionButtonStyle}"
                                        Click="Edit_Click"/>
                                <Button Content="🗑️ حذف"
                                        Background="#EF5350"
                                        Foreground="White"
                                        Style="{StaticResource GridActionButtonStyle}"
                                        Click="Delete_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
