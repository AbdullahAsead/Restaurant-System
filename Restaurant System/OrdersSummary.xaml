<Window x:Class="Restaurant_System.OrdersSummary"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant_System"
        mc:Ignorable="d"
        Title="ملخص الطلبات"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Grid Margin="20">
        <StackPanel>

            <!-- عنوان الصفحة -->
            <TextBlock Text="ملخص الطلبات"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#1976D2"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- جدول الطلبات -->
            <DataGrid x:Name="OrdersGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      RowHeight="35"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Margin="0,0,0,10">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="رقم" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding Date}" Width="130"/>
                    <DataGridTextColumn Header="نوع" Binding="{Binding OrderType}" Width="80"/>
                    <DataGridTextColumn Header="العميل" Binding="{Binding CustomerName}" Width="140"/>
                    <DataGridTextColumn Header="الطاولة" Binding="{Binding TableId}" Width="80"/>
                    <DataGridTextColumn Header="المندوب" Binding="{Binding DeliveryPerson}" Width="130"/>
                    <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalPrice}" Width="90"/>

                    <DataGridTemplateColumn Header="تحكم" Width="220">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="عرض"
                                            Background="#03A9F4"
                                            Foreground="White"
                                            Padding="5"
                                            Margin="2"
                                            Width="60"
                                            Click="View_Click"/>

                                    <Button Content="🖨️"
                                            Background="#4CAF50"
                                            Foreground="White"
                                            Padding="5"
                                            Margin="2"
                                            Width="40"
                                            Click="Print_Click"/>

                                    <Button Content="🗑️"
                                            Background="#F44336"
                                            Foreground="White"
                                            Padding="5"
                                            Margin="2"
                                            Width="40"
                                            Click="Delete_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
